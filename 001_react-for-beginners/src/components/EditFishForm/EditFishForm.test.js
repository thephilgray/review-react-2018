import React from 'react';
import { shallow } from 'enzyme';
import EditFishForm from './EditFishForm';

describe.skip('EditFishForm', () => {
  describe('when updating a field', () => {
    it('should update the state for that item', () => {});
  });
});
